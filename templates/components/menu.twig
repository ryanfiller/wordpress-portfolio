{% if menu %}
	<ul class="nav" role="navigation">
	{% for item in menu %}
		<li class="{{item.classes | join(' ')}} nav__link">
			<a href="{{item.get_link}}">{{item.title}}</a>
		</li>
	{% endfor %}
	</ul>
{% endif %}
